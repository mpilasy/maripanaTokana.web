services:
  web:
    build: .
    container_name: maripanaTokana.web
    restart: unless-stopped
    ports:
      - "${PORT:-3080}:80"
    environment:
      # Which app to serve at / (svelte, react, or ng)
      - DEFAULT_APP=${DEFAULT_APP:-svelte}
